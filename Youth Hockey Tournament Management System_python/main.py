from user_interface import UserInterface  # Rename class to follow PascalCase

if __name__ == "__main__": # Ensure the script runs only when executed directly
    ui = UserInterface()  ## Create an instance of the UserInterface class
    ui.run() # Start the user interface1